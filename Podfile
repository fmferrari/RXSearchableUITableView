# Uncomment the next line to define a global platform for your project
 platform :ios, '10.0'

target 'ReactiveSearchableUITableView' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ReactiveSearchableUITableView
  pod "RxSwift"
  pod "RxCocoa"

  target 'ReactiveSearchableUITableViewTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
